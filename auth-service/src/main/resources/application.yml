spring:
  application:
    name: auth-service

  config:
    import: optional:configserver:http://localhost:8888

  profiles:
    active: dev

  eureka:
    instance:
      prefer-ip-address: true
      ip-address: 127.0.0.1